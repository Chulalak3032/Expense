<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.usablelabs.duedo">

    <application
        android:name=".DueDoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Expense"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FormActivity" />
        <activity android:name=".ShowActivity" />
        <activity android:name=".ShowExpenseActivity" />

        <meta-data
            android:name="AA_DB_NAME"
            android:value="DueDu.db" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="1" />
        <meta-data
            android:name="AA_MODELS"
            android:value="org.usablelabs.duedo.Task,org.usablelabs.duedo.Expense,org.usablelabs.duedo.Income" />

        <activity android:name=".ReportActivity" />
        <activity android:name=".FormExpenseActivity" />
        <activity android:name=".FormIncomeActivity" />
        <activity android:name=".Form2ExpenseActivity" />
        <activity android:name=".IncomeActivity" />
        <activity android:name=".BarChartActivity" />
        <activity android:name=".LineChartActivity" />
        <activity android:name=".ShowIncomeActivity" />
        <activity android:name=".ExpenseActivity"></activity>
    </application>

</manifest>
